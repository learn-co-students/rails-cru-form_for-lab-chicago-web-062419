<h1>Songs#show</h1>
<h1><%= @song.name %></h1>
<h2><%= link_to "#{@song.artist.name}", artist_path(@song.artist) %><br></h2>
<h2><%= link_to "#{@song.genre.name}", genre_path(@song.genre) %><br></h2>

<%= link_to 'Edit Song', edit_song_path %><br>
<%= link_to 'Back to Home', songs_path %>